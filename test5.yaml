apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-test5
  description: kubernetes service
  annotations:
    'backstage.io/kubernetes-namespace': 'app.kubernetes.io/namespace=vp-player'

spec:
  type: service
  lifecycle: production
  owner: user:default/guest
